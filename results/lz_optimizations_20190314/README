20200506:
	Trying to rerun the fidelities for these results I don't get the same fidelities. These are probably have been computed with the wrong qutip fidelity function, hence the fidelities larger than one.